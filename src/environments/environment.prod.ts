// Archivo de entorno para producción
// La URL se puede sobrescribir con variable de entorno durante el build
// En Render, usa la variable de entorno NG_APP_API_URL
export const environment = {
  production: true,
  apiUrl: 'https://appweb-be.onrender.com'
};

